# MGEMS – Gemini CLI Instructions

## Project Overview
MGEMS is a web-based interview assistant (Node.js/Express backend + vanilla JS frontend).  
Live URL: https://mgems-ai-interviewer.onrender.com

## Technology Stack
- Node.js + Express server
- Vanilla JavaScript (no build step) Render.com hosting (PORT auto-assigned)
- GitHub Spec Kit for spec-driven development

## Key Commands
```bash
npm install          # install deps
npm start            # start server (PORT from env or 3000)
```

## Spec Kit Integration
- `.specify/` holds all SDD artefacts
- Use `/speckit.*` slash commands in any file to invoke Spec Kit workflows
- Gemini CLI is the default AI agent (`gemini-here.cmd` wrapper loads API key from `.env`)

## Coding Conventions
- Keep routes lean; heavy logic goes in separate modules under `routes/`
- Use `console.log` for quick debugging (Render logs capture stdout)
- Commit `package-lock.json`; ignore `.env` except `.env.example`

## Recent Changes
- main: Added GitHub Spec Kit + Gemini CLI integration

---
Generated by Spec Kit on 2026-02-13